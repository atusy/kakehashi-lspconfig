# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.ada_ls]
cmd = ["ada_language_server"]
languages = ["ada"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.agda_ls]
cmd = ["als"]
languages = ["agda"]
[languageServers.aiken]
cmd = ["aiken", "lsp"]
languages = ["aiken"]
rootMarkers = ["aiken.toml", ".git"]
[languageServers.air]
cmd = ["air", "language-server"]
languages = ["r"]
rootMarkers = ["air.toml", ".air.toml", ".git"]
[languageServers.alloy_ls]
cmd = ["alloy", "lsp"]
languages = ["alloy"]
rootMarkers = [".git"]
[languageServers.anakin_language_server]
cmd = ["anakinls"]
languages = ["python"]
rootMarkers = ["pyproject.toml", "setup.py", "setup.cfg", "requirements.txt", "Pipfile", ".git"]
settings = { anakinls = { pyflakes_errors = ["ImportStarNotPermitted", "UndefinedExport", "UndefinedLocal", "UndefinedName", "DuplicateArgument", "MultiValueRepeatedKeyLiteral", "MultiValueRepeatedKeyVariable", "FutureFeatureNotDefined", "LateFutureImport", "ReturnOutsideFunction", "YieldOutsideFunction", "ContinueOutsideLoop", "BreakOutsideLoop", "TwoStarredExpressions", "TooManyExpressionsInStarredAssignment", "ForwardAnnotationSyntaxError", "RaiseNotImplemented", "StringDotFormatExtraPositionalArguments", "StringDotFormatExtraNamedArguments", "StringDotFormatMissingArgument", "StringDotFormatMixingAutomatic", "StringDotFormatInvalidFormat", "PercentFormatInvalidFormat", "PercentFormatMixedPositionalAndNamed", "PercentFormatUnsupportedFormat", "PercentFormatPositionalCountMismatch", "PercentFormatExtraNamedArguments", "PercentFormatMissingArgument", "PercentFormatExpectedMapping", "PercentFormatExpectedSequence", "PercentFormatStarRequiresSequence"] } }
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.angularls]
languages = ["typescript", "html", "typescriptreact", "htmlangular"]
rootMarkers = ["angular.json", "nx.json"]
[languageServers.ansiblels]
cmd = ["ansible-language-server", "--stdio"]
languages = ["yaml.ansible"]
rootMarkers = ["ansible.cfg", ".ansible-lint"]
settings = { ansible = { ansible = { path = "ansible" }, executionEnvironment = { enabled = false }, python = { interpreterPath = "python" }, validation = { enabled = true, lint = { enabled = true, path = "ansible-lint" } } } }
[languageServers.antlersls]
cmd = ["antlersls", "--stdio"]
languages = ["html", "antlers"]
rootMarkers = ["composer.json"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.apex_ls]
languages = ["apex", "apexcode"]
rootMarkers = ["sfdx-project.json"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
[languageServers.arduino_language_server]
cmd = ["arduino-language-server"]
languages = ["arduino"]
[languageServers.asm_lsp]
cmd = ["asm-lsp"]
languages = ["asm", "vmasm"]
rootMarkers = [".asm-lsp.toml", ".git"]
# WARN: reuse_client was dropped; not applicable to kakehashi
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.ast_grep]
cmd = ["ast-grep", "lsp"]
languages = ["bash", "c", "cpp", "cs", "css", "elixir", "go", "haskell", "html", "java", "javascript", "javascriptreact", "json", "kotlin", "lua", "nix", "php", "python", "ruby", "rust", "scala", "solidity", "swift", "typescript", "typescriptreact", "yaml"]
rootMarkers = ["sgconfig.yaml", "sgconfig.yml"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: before_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.astro]
languages = ["astro"]
rootMarkers = ["package.json", "tsconfig.json", "jsconfig.json", ".git"]
initializationOptions = { typescript = {} }
[languageServers.atlas]
cmd = ["atlas", "tool", "lsp", "--stdio"]
languages = ["atlas-*"]
rootMarkers = ["atlas.hcl"]
[languageServers.atopile]
cmd = ["ato", "lsp", "start"]
languages = ["ato"]
rootMarkers = ["ato.yaml", ".ato", ".git"]
# WARN: unhandled field `flags` (table) was dropped
[languageServers.autohotkey_lsp]
cmd = ["autohotkey_lsp", "--stdio"]
languages = ["autohotkey"]
rootMarkers = ["package.json"]
initializationOptions = { ActionWhenV1IsDetected = "Continue", AutoLibInclude = "All", CommentTags = "^;;\\s*(?<tag>.+)", CompleteFunctionParens = false, Diagnostics = { ClassStaticMemberCheck = true, ParamsCheck = true }, FormatOptions = { array_style = "expand", brace_style = "One True Brace", break_chained_methods = false, ignore_comment = false, indent_string = "\t", max_preserve_newlines = 2, object_style = "none", preserve_newlines = true, space_after_double_colon = true, space_before_conditional = true, space_in_empty_paren = false, space_in_other = true, space_in_paren = false, wrap_line_length = 0 }, InterpreterPath = "", SymbolFoldinFromOpenBrace = false, locale = "en-us" }
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.autotools_ls]
cmd = ["autotools-language-server"]
languages = ["config", "automake", "make"]
[languageServers.awk_ls]
cmd = ["awk-language-server"]
languages = ["awk"]
[languageServers.azure_pipelines_ls]
cmd = ["azure-pipelines-language-server", "--stdio"]
languages = ["yaml"]
rootMarkers = ["azure-pipelines.yml"]
[languageServers.bacon_ls]
cmd = ["bacon-ls"]
languages = ["rust"]
rootMarkers = [".bacon-locations", "Cargo.toml"]
[languageServers.ballerina]
cmd = ["bal", "start-language-server"]
languages = ["ballerina"]
rootMarkers = ["Ballerina.toml"]
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.basedpyright]
cmd = ["basedpyright-langserver", "--stdio"]
languages = ["python"]
rootMarkers = ["pyrightconfig.json", "pyproject.toml", "setup.py", "setup.cfg", "requirements.txt", "Pipfile", ".git"]
settings = { basedpyright = { analysis = { autoSearchPaths = true, diagnosticMode = "openFilesOnly" } } }
[languageServers.bashls]
cmd = ["bash-language-server", "start"]
languages = ["bash", "sh"]
rootMarkers = [".git"]
settings = { bashIde = { globPattern = "*@(.sh|.inc|.bash|.command)" } }
# WARN: no `filetypes` in source; set `languages` manually or this server never matches
[languageServers.basics_ls]
cmd = ["basics-language-server"]
settings = { buffer = { enable = true, minCompletionLength = 4 }, path = { enable = true }, snippet = { enable = false, sources = {} } }
[languageServers.bazelrc_lsp]
cmd = ["bazelrc-lsp", "lsp"]
languages = ["bazelrc"]
rootMarkers = ["WORKSPACE", "WORKSPACE.bazel", "MODULE.bazel"]
[languageServers.beancount]
cmd = ["beancount-language-server", "--stdio"]
languages = ["beancount", "bean"]
rootMarkers = [".git"]
# WARN: no `cmd` in source; set `cmd` manually or kakehashi will skip this server
[languageServers.bicep]
languages = ["bicep", "bicep-params"]
rootMarkers = [".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.biome]
languages = ["astro", "css", "graphql", "html", "javascript", "javascriptreact", "json", "jsonc", "svelte", "typescript", "typescriptreact", "vue"]
[languageServers.bitbake_language_server]
cmd = ["bitbake-language-server"]
languages = ["bitbake"]
rootMarkers = [".git"]
# WARN: cmd_env was dropped; kakehashi has no per-server environment option
[languageServers.blueprint_ls]
cmd = ["blueprint-compiler", "lsp"]
languages = ["blueprint"]
rootMarkers = [".git"]
[languageServers.bqls]
cmd = ["bqls"]
languages = ["sql"]
rootMarkers = [".git"]
[languageServers.bright_script]
cmd = ["bsc", "--lsp", "--stdio"]
languages = ["brs"]
rootMarkers = ["makefile", "Makefile", ".git"]
[languageServers.brioche]
cmd = ["brioche", "lsp"]
languages = ["brioche"]
rootMarkers = ["project.bri"]
# WARN: no `cmd` in source; set `cmd` manually or kakehashi will skip this server
[languageServers.bsl_ls]
languages = ["bsl", "os"]
rootMarkers = [".git"]
[languageServers.buck2]
cmd = ["buck2", "lsp"]
languages = ["bzl"]
rootMarkers = [".buckconfig"]
[languageServers.buddy_ls]
cmd = ["buddy-lsp-server"]
languages = ["mlir"]
rootMarkers = [".git"]
# WARN: reuse_client was dropped; not applicable to kakehashi
[languageServers.buf_ls]
cmd = ["buf", "lsp", "serve", "--log-format=text"]
languages = ["proto", "buf-config"]
rootMarkers = ["buf.yaml", ".git"]
[languageServers.bzl]
cmd = ["bzl", "lsp", "serve"]
languages = ["bzl"]
rootMarkers = ["WORKSPACE", "WORKSPACE.bazel"]
[languageServers.c3_lsp]
cmd = ["c3lsp"]
languages = ["c3", "c3i"]
rootMarkers = ["project.json", "manifest.json", ".git"]
[languageServers.cairo_ls]
cmd = ["scarb", "cairo-language-server", "/C", "--node-ipc"]
languages = ["cairo"]
rootMarkers = ["Scarb.toml", "cairo_project.toml", ".git"]
initializationOptions = { hostInfo = "neovim" }
# WARN: offset_encoding was dropped; kakehashi manages offset encoding
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.ccls]
cmd = ["ccls"]
languages = ["c", "cpp", "objc", "objcpp", "cuda"]
rootMarkers = ["compile_commands.json", ".ccls", ".git"]
[languageServers.cds_lsp]
cmd = ["cds-lsp", "--stdio"]
languages = ["cds"]
rootMarkers = ["package.json", "db", "srv"]
settings = { cds = { validate = true } }
[languageServers.cir_lsp_server]
cmd = ["cir-lsp-server"]
languages = ["cir"]
rootMarkers = [".git"]
[languageServers.circom-lsp]
cmd = ["circom-lsp"]
languages = ["circom"]
rootMarkers = [".git"]
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
# WARN: get_language_id (Lua function) was dropped
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
# WARN: on_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.clangd]
cmd = ["clangd"]
languages = ["c", "c.doxygen", "cpp", "cpp.doxygen", "objc", "objcpp", "cuda"]
rootMarkers = [".clangd", ".clang-tidy", ".clang-format", "compile_commands.json", "compile_flags.txt", "configure.ac", ".git"]
[languageServers.clarinet]
cmd = ["clarinet", "lsp"]
languages = ["clar", "clarity"]
rootMarkers = ["Clarinet.toml"]
[languageServers.clojure_lsp]
cmd = ["clojure-lsp"]
languages = ["clojure", "edn"]
rootMarkers = ["project.clj", "deps.edn", "build.boot", "shadow-cljs.edn", ".git", "bb.edn"]
[languageServers.cmake]
cmd = ["cmake-language-server"]
languages = ["cmake"]
rootMarkers = ["CMakePresets.json", "CTestConfig.cmake", ".git", "build", "cmake"]
initializationOptions = { buildDirectory = "build" }
[languageServers.cobol_ls]
cmd = ["cobol-language-support"]
languages = ["cobol"]
rootMarkers = [".git"]
[languageServers.codebook]
cmd = ["codebook-lsp", "serve"]
languages = ["c", "css", "gitcommit", "go", "haskell", "html", "java", "javascript", "javascriptreact", "lua", "markdown", "php", "python", "ruby", "rust", "swift", "toml", "text", "typescript", "typescriptreact", "zig"]
rootMarkers = [".git", "codebook.toml", ".codebook.toml"]
[languageServers.coffeesense]
cmd = ["coffeesense-language-server", "--stdio"]
languages = ["coffee"]
rootMarkers = ["package.json"]
# WARN: no `filetypes` in source; set `languages` manually or this server never matches
[languageServers.contextive]
cmd = ["Contextive.LanguageServer"]
rootMarkers = [".contextive", ".git"]
# WARN: no `filetypes` in source; set `languages` manually or this server never matches
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.copilot]
cmd = ["copilot-language-server", "--stdio"]
rootMarkers = [".git"]
initializationOptions = { editorInfo = { name = "Neovim", version = "0.13.0-dev" }, editorPluginInfo = { name = "Neovim", version = "0.13.0-dev" } }
settings = { telemetry = { telemetryLevel = "all" } }
[languageServers.coq_lsp]
cmd = ["coq-lsp"]
languages = ["coq"]
rootMarkers = ["_CoqProject", ".git"]
[languageServers.cqlls]
cmd = ["cqlls"]
languages = ["cql", "cqlang"]
rootMarkers = [".cqlls", ".git"]
[languageServers.crystalline]
cmd = ["crystalline"]
languages = ["crystal"]
rootMarkers = ["shard.yml", ".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: get_language_id (Lua function) was dropped
[languageServers.csharp_ls]
languages = ["cs"]
initializationOptions = { AutomaticWorkspaceInit = true }
# WARN: no `filetypes` in source; set `languages` manually or this server never matches
[languageServers.cspell_ls]
cmd = ["cspell-lsp", "--stdio"]
rootMarkers = [".git", "cspell.json", ".cspell.json", "cspell.json", ".cSpell.json", "cSpell.json", "cspell.config.js", "cspell.config.cjs", "cspell.config.json", "cspell.config.yaml", "cspell.config.yml", "cspell.yaml", "cspell.yml"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.css_variables]
cmd = ["css-variables-language-server", "--stdio"]
languages = ["css", "scss", "less"]
settings = { cssVariables = { blacklistFolders = ["**/.cache", "**/.DS_Store", "**/.git", "**/.hg", "**/.next", "**/.svn", "**/bower_components", "**/CVS", "**/dist", "**/node_modules", "**/tests", "**/tmp"], lookupFiles = ["**/*.less", "**/*.scss", "**/*.sass", "**/*.css"] } }
[languageServers.csskit]
cmd = ["csskit", "lsp"]
languages = ["css"]
rootMarkers = ["package.json", ".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.cssls]
languages = ["css", "scss", "less"]
rootMarkers = ["package.json", ".git"]
initializationOptions = { provideFormatter = true }
settings = { css = { validate = true }, less = { validate = true }, scss = { validate = true } }
[languageServers.cssmodules_ls]
cmd = ["cssmodules-language-server"]
languages = ["javascript", "javascriptreact", "typescript", "typescriptreact"]
rootMarkers = ["package.json"]
# WARN: no `filetypes` in source; set `languages` manually or this server never matches
[languageServers.ctags_lsp]
cmd = ["ctags-lsp"]
rootMarkers = ["tags", ".tags", ".git"]
[languageServers.cucumber_language_server]
cmd = ["cucumber-language-server", "--stdio"]
languages = ["cucumber"]
rootMarkers = [".git"]
[languageServers.cue]
cmd = ["cue", "lsp"]
languages = ["cue"]
rootMarkers = ["cue.mod", ".git"]
# WARN: no `filetypes` in source; set `languages` manually or this server never matches
[languageServers.custom_elements_ls]
cmd = ["custom-elements-languageserver", "--stdio"]
rootMarkers = ["tsconfig.json", "package.json", "jsconfig.json", ".git"]
initializationOptions = { hostInfo = "neovim" }
[languageServers.cypher_ls]
cmd = ["cypher-language-server", "--stdio"]
languages = ["cypher"]
rootMarkers = [".git"]
[languageServers.daedalus_ls]
cmd = ["DaedalusLanguageServer"]
languages = ["d"]
rootMarkers = ["Gothic.src", "Camera.src", "Menu.src", "Music.src", "ParticleFX.src", "SFX.src", "VisualFX.src"]
settings = { DaedalusLanguageServer = { fileEncoding = "Windows-1252", inlayHints = { constants = true }, loglevel = "debug", numParserThreads = 16, srcFileEncoding = "Windows-1252" } }
[languageServers.dafny]
cmd = ["dafny", "server"]
languages = ["dfy", "dafny"]
rootMarkers = [".git"]
[languageServers.dagger]
cmd = ["cuelsp"]
languages = ["cue"]
rootMarkers = ["cue.mod", ".git"]
[languageServers.dartls]
cmd = ["dart", "language-server", "--protocol=lsp"]
languages = ["dart"]
rootMarkers = ["pubspec.yaml"]
initializationOptions = { closingLabels = true, flutterOutline = true, onlyAnalyzeProjectsWithOpenFiles = true, outline = true, suggestFromUnimportedLibraries = true }
settings = { dart = { completeFunctionCalls = true, showTodos = true } }
[languageServers.dcmls]
cmd = ["dcm", "start-server", "--client=neovim"]
languages = ["dart"]
rootMarkers = ["pubspec.yaml"]
[languageServers.debputy]
cmd = ["debputy", "lsp", "server"]
languages = ["debcontrol", "debcopyright", "debchangelog", "autopkgtest", "make", "yaml"]
rootMarkers = ["debian"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: cmd_env was dropped; kakehashi has no per-server environment option
# WARN: handlers (client-side LSP handlers) were dropped
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.denols]
cmd = ["deno", "lsp"]
languages = ["javascript", "javascriptreact", "typescript", "typescriptreact"]
settings = { deno = { enable = true, suggest = { imports = { hosts = { "https://deno.land" = true } } } } }
[languageServers.dexter]
cmd = ["dexter", "lsp"]
languages = ["elixir", "eelixir", "heex"]
rootMarkers = [".dexter/dexter.db", ".dexter.db", ".git", "mix.exs"]
initializationOptions = { followDelegates = true }
[languageServers.dhall_lsp_server]
cmd = ["dhall-lsp-server"]
languages = ["dhall"]
rootMarkers = [".git"]
# WARN: filetypes was empty in source; set `languages` manually or this server never matches
[languageServers.diagnosticls]
cmd = ["diagnostic-languageserver", "--stdio"]
rootMarkers = [".git"]
[languageServers.digestif]
cmd = ["digestif"]
languages = ["tex", "plaintex", "context"]
rootMarkers = [".git"]
[languageServers.djls]
cmd = ["djls", "serve"]
languages = ["htmldjango", "html", "python"]
rootMarkers = ["manage.py", "pyproject.toml", ".git"]
[languageServers.djlsp]
cmd = ["djlsp"]
languages = ["html", "htmldjango"]
rootMarkers = [".git"]
[languageServers.docker_compose_language_service]
cmd = ["docker-compose-langserver", "--stdio"]
languages = ["yaml.docker-compose"]
rootMarkers = ["docker-compose.yaml", "docker-compose.yml", "compose.yaml", "compose.yml"]
# WARN: get_language_id (Lua function) was dropped
[languageServers.docker_language_server]
cmd = ["docker-language-server", "start", "--stdio"]
languages = ["dockerfile", "yaml.docker-compose"]
rootMarkers = ["Dockerfile", "docker-compose.yaml", "docker-compose.yml", "compose.yaml", "compose.yml", "docker-bake.json", "docker-bake.hcl", "docker-bake.override.json", "docker-bake.override.hcl"]
[languageServers.dockerls]
cmd = ["docker-langserver", "--stdio"]
languages = ["dockerfile"]
rootMarkers = ["Dockerfile"]
[languageServers.dolmenls]
cmd = ["dolmenls"]
languages = ["smt2", "tptp", "p", "cnf", "icnf", "zf"]
rootMarkers = [".git"]
[languageServers.dotls]
cmd = ["dot-language-server", "--stdio"]
languages = ["dot"]
rootMarkers = [".git"]
[languageServers.dprint]
cmd = ["dprint", "lsp"]
languages = ["javascript", "javascriptreact", "typescript", "typescriptreact", "json", "jsonc", "markdown", "python", "toml", "rust", "roslyn", "graphql"]
rootMarkers = ["dprint.json", ".dprint.json", "dprint.jsonc", ".dprint.jsonc"]
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.ds_pinyin_lsp]
cmd = ["ds-pinyin-lsp"]
languages = ["markdown", "org"]
rootMarkers = [".git"]
initializationOptions = { completion_on = true, match_as_same_as_input = true, match_long_input = true, max_suggest = 15, show_symbols = true, show_symbols_by_n_times = 0, show_symbols_only_follow_by_hanzi = false }
[languageServers.dts_lsp]
cmd = ["dts-lsp"]
languages = ["dts", "dtsi", "overlay"]
rootMarkers = [".git"]
[languageServers.earthlyls]
cmd = ["earthlyls"]
languages = ["earthfile"]
rootMarkers = ["Earthfile"]
[languageServers.ecsact]
cmd = ["ecsact_lsp_server", "--stdio"]
languages = ["ecsact"]
rootMarkers = [".git"]
# WARN: no `filetypes` in source; set `languages` manually or this server never matches
[languageServers.efm]
cmd = ["efm-langserver"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.elixirls]
cmd = ["elixir-ls"]
languages = ["elixir", "eelixir", "heex", "surface"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
[languageServers.elmls]
cmd = ["elm-language-server"]
languages = ["elm"]
initializationOptions = { disableElmLSDiagnostics = false, elmReviewDiagnostics = "off", onlyUpdateDiagnosticsOnSave = false, skipInstallPackageConfirmation = false }
[languageServers.elp]
cmd = ["elp", "server"]
languages = ["erlang"]
rootMarkers = ["rebar.config", "erlang.mk", ".git"]
[languageServers.ember]
cmd = ["ember-language-server", "--stdio"]
languages = ["handlebars", "typescript", "javascript", "typescript.glimmer", "javascript.glimmer"]
rootMarkers = ["ember-cli-build.js", ".git"]
[languageServers.emmet_language_server]
cmd = ["emmet-language-server", "--stdio"]
languages = ["astro", "css", "eruby", "html", "htmlangular", "htmldjango", "javascriptreact", "less", "sass", "scss", "svelte", "typescriptreact", "vue"]
rootMarkers = [".git"]
[languageServers.emmet_ls]
cmd = ["emmet-ls", "--stdio"]
languages = ["astro", "css", "eruby", "html", "htmlangular", "htmldjango", "javascriptreact", "less", "pug", "sass", "scss", "svelte", "templ", "typescriptreact", "vue"]
rootMarkers = [".git"]
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.emmylua_ls]
cmd = ["emmylua_ls"]
languages = ["lua"]
rootMarkers = [[".emmyrc.json", ".emmyrc.lua", ".luarc.json", ".luarc.jsonc"], [".luacheckrc", ".stylua.toml", "stylua.toml", "selene.toml", "selene.yml"], ".git"]
settings = { emmylua = { codeLens = { enable = true }, hint = { enable = true } } }
[languageServers.erg_language_server]
cmd = ["erg", "--language-server"]
languages = ["erg"]
rootMarkers = ["package.er", ".git"]
[languageServers.esbonio]
cmd = ["python3", "-m", "esbonio.server"]
languages = ["rst"]
rootMarkers = [".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: before_init (editor-side hook) was dropped; not applicable to kakehashi
# WARN: handlers (client-side LSP handlers) were dropped
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.eslint]
languages = ["javascript", "javascriptreact", "typescript", "typescriptreact", "vue", "svelte", "astro", "htmlangular"]
settings = { codeAction = { disableRuleComment = { enable = true, location = "separateLine" }, showDocumentation = { enable = true } }, codeActionOnSave = { enable = false, mode = "all" }, experimental = {}, format = true, nodePath = "", onIgnoredFiles = "off", problems = { shortenToSingleLine = false }, quiet = false, rulesCustomizations = {}, run = "onType", useESLintClass = false, validate = "on", workingDirectory = { mode = "auto" } }
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.expert]
cmd = ["expert", "--stdio"]
languages = ["elixir", "eelixir", "heex", "surface"]
[languageServers.facility_language_server]
cmd = ["facility-language-server"]
languages = ["fsd"]
rootMarkers = [".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.fallow]
languages = ["javascript", "typescript", "javascriptreact", "typescriptreact"]
rootMarkers = [".fallowrc.json", ".git"]
[languageServers.fennel_language_server]
cmd = ["fennel-language-server"]
languages = ["fennel"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
[languageServers.fennel_ls]
cmd = ["fennel-ls"]
languages = ["fennel"]
[languageServers.fish_lsp]
cmd = ["fish-lsp", "start"]
languages = ["fish"]
rootMarkers = ["config.fish", ".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.flow]
languages = ["javascript", "javascriptreact"]
rootMarkers = [".flowconfig"]
[languageServers.flux_lsp]
cmd = ["flux-lsp"]
languages = ["flux"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.foam_ls]
cmd = ["foam-ls", "--stdio"]
languages = ["foam", "OpenFOAM"]
[languageServers.fortitude]
cmd = ["fortitude", "server"]
languages = ["fortran"]
rootMarkers = ["fpm.toml", "fortitude.toml", ".fortitude.toml", ".git"]
[languageServers.fortls]
cmd = ["fortls", "--notify_init", "--hover_signature", "--hover_language=fortran", "--use_signature_help"]
languages = ["fortran"]
rootMarkers = [".fortls", ".fortlsrc", ".fortls.json", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.fsautocomplete]
cmd = ["fsautocomplete", "--adaptive-lsp-server-enabled"]
languages = ["fsharp"]
initializationOptions = { AutomaticWorkspaceInit = true }
settings = { FSharp = { EnableReferenceCodeLens = true, ExternalAutocomplete = false, InterfaceStubGeneration = true, InterfaceStubGenerationMethodBody = "failwith \"Not Implemented\"", InterfaceStubGenerationObjectIdentifier = "this", Linter = true, RecordStubGeneration = true, RecordStubGenerationBody = "failwith \"Not Implemented\"", ResolveNamespaces = true, SimplifyNameAnalyzer = true, UnionCaseStubGeneration = true, UnionCaseStubGenerationBody = "failwith \"Not Implemented\"", UnusedDeclarationsAnalyzer = true, UnusedOpensAnalyzer = true, UseSdkScripts = true, keywordsAutocomplete = true } }
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.fsharp_language_server]
cmd = ["dotnet", "FSharpLanguageServer.dll"]
languages = ["fsharp"]
initializationOptions = { AutomaticWorkspaceInit = true }
[languageServers.fstar]
cmd = ["fstar.exe", "--lsp"]
languages = ["fstar"]
rootMarkers = [".git"]
[languageServers.futhark_lsp]
cmd = ["futhark", "lsp"]
languages = ["futhark", "fut"]
rootMarkers = [".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.gdscript]
languages = ["gdscript"]
rootMarkers = ["project.godot", ".git"]
[languageServers.gdshader_lsp]
cmd = ["gdshader-lsp", "--stdio"]
languages = ["gdshader", "gdshaderinc"]
rootMarkers = ["project.godot"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
# WARN: handlers (client-side LSP handlers) were dropped
[languageServers.gh_actions_ls]
cmd = ["gh-actions-language-server", "--stdio"]
languages = ["yaml"]
[languageServers.ghcide]
cmd = ["ghcide", "--lsp"]
languages = ["haskell", "lhaskell"]
rootMarkers = ["stack.yaml", "hie-bios", "BUILD.bazel", "cabal.config", "package.yaml"]
[languageServers.ghdl_ls]
cmd = ["ghdl-ls"]
languages = ["vhdl"]
rootMarkers = ["hdl-prj.json", ".git"]
[languageServers.ginko_ls]
cmd = ["ginko_ls"]
languages = ["dts"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.gitlab_ci_ls]
cmd = ["gitlab-ci-ls"]
languages = ["yaml.gitlab"]
initializationOptions = { cache_path = "/Users/atusy/.cache/gitlab-ci-ls/", log_path = "/Users/atusy/.cache/gitlab-ci-ls//log/gitlab-ci-ls.log" }
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
# WARN: on_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.gitlab_duo]
cmd = ["npx", "--@gitlab-org:registry=https://gitlab.com/api/v4/packages/npm/", "@gitlab-org/gitlab-lsp", "--stdio"]
languages = ["ruby", "go", "javascript", "typescript", "typescriptreact", "javascriptreact", "rust", "lua", "python", "java", "cpp", "c", "php", "cs", "kotlin", "swift", "scala", "vue", "svelte", "html", "css", "scss", "json", "yaml"]
rootMarkers = [".git"]
initializationOptions = { editorInfo = { name = "Neovim", version = "0.13.0-dev" }, editorPluginInfo = { name = "Neovim LSP", version = "0.13.0-dev" }, extension = { name = "Neovim LSP Client", version = "0.13.0-dev" }, ide = { name = "Neovim", vendor = "Neovim", version = "0.13.0-dev" } }
settings = { baseUrl = "https://gitlab.com", codeCompletion = { enableSecretRedaction = true }, featureFlags = { streamCodeGenerations = false }, logLevel = "info", telemetry = { enabled = false } }
[languageServers.glasgow]
cmd = ["glasgow"]
languages = ["wgsl"]
rootMarkers = [".git"]
[languageServers.gleam]
cmd = ["gleam", "lsp"]
languages = ["gleam"]
rootMarkers = ["gleam.toml", ".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.glint]
languages = ["html.handlebars", "handlebars", "typescript", "typescript.glimmer", "javascript", "javascript.glimmer"]
rootMarkers = [".glintrc.yml", ".glintrc", ".glintrc.json", ".glintrc.js", "glint.config.js", "package.json"]
initializationOptions = { glint = { useGlobal = false } }
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
[languageServers.glsl_analyzer]
cmd = ["glsl_analyzer"]
languages = ["glsl", "vert", "tesc", "tese", "frag", "geom", "comp"]
rootMarkers = [".git"]
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
[languageServers.glslls]
cmd = ["glslls", "--stdin"]
languages = ["glsl", "vert", "tesc", "tese", "frag", "geom", "comp"]
rootMarkers = [".git"]
[languageServers.gn_language_server]
cmd = ["gn-language-server", "--stdio"]
languages = ["gn"]
rootMarkers = [".gn", ".git"]
[languageServers.gnls]
cmd = ["gnls", "--stdio"]
languages = ["gn"]
rootMarkers = [".gn", ".git"]
# WARN: before_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.golangci_lint_ls]
cmd = ["golangci-lint-langserver"]
languages = ["go", "gomod"]
rootMarkers = [".golangci.yml", ".golangci.yaml", ".golangci.toml", ".golangci.json", "go.work", "go.mod", ".git"]
initializationOptions = { command = ["golangci-lint", "run", "--output.text.path=", "--output.tab.path=", "--output.html.path=", "--output.checkstyle.path=", "--output.junit-xml.path=", "--output.teamcity.path=", "--output.sarif.path=", "--show-stats=false", "--output.json.path=stdout"] }
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.gopls]
cmd = ["gopls"]
languages = ["go", "gomod", "gowork", "gotmpl"]
settings = { gopls = { semanticTokens = true } }
[languageServers.gradle_ls]
cmd = ["gradle-language-server"]
languages = ["groovy"]
rootMarkers = ["settings.gradle", "build.gradle"]
initializationOptions = { settings = { gradleWrapperEnabled = true } }
# WARN: handlers (client-side LSP handlers) were dropped
[languageServers.grammarly]
cmd = ["grammarly-languageserver", "--stdio"]
languages = ["markdown"]
rootMarkers = [".git"]
initializationOptions = { clientId = "client_BaDkMgx4X19X9UxxYRCXZo" }
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.graphql]
cmd = ["graphql-lsp", "server", "-m", "stream"]
languages = ["graphql", "typescriptreact", "javascriptreact"]
[languageServers.groovyls]
cmd = ["groovy-language-server"]
languages = ["groovy"]
rootMarkers = ["Jenkinsfile", ".git"]
[languageServers.guile_ls]
cmd = ["guile-lsp-server"]
languages = ["scheme.guile"]
rootMarkers = ["guix.scm", ".git"]
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.hare_lsp]
cmd = ["hare-lsp", "-S"]
languages = ["hare"]
rootMarkers = [".git"]
[languageServers.harper_ls]
cmd = ["harper-ls", "--stdio"]
languages = ["asciidoc", "c", "cpp", "cs", "gitcommit", "go", "html", "java", "javascript", "lua", "markdown", "nix", "python", "ruby", "rust", "swift", "tex", "toml", "typescript", "typescriptreact", "haskell", "cmake", "typst", "php", "dart", "clojure", "sh"]
rootMarkers = [".harper-dictionary.txt", ".git"]
[languageServers.hdl_checker]
cmd = ["hdl_checker", "--lsp"]
languages = ["vhdl", "verilog", "systemverilog"]
rootMarkers = [".git"]
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
[languageServers.helm_ls]
cmd = ["helm_ls", "serve"]
languages = ["helm", "yaml.helm-values"]
rootMarkers = ["Chart.yaml"]
[languageServers.herb_ls]
cmd = ["herb-language-server", "--stdio"]
languages = ["html", "eruby"]
rootMarkers = ["Gemfile", ".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.hhvm]
languages = ["php", "hack"]
rootMarkers = [".hhconfig"]
[languageServers.hie]
cmd = ["hie-wrapper", "--lsp"]
languages = ["haskell"]
rootMarkers = ["stack.yaml", "package.yaml", ".git"]
[languageServers.hlasm]
cmd = ["hlasm_language_server"]
languages = ["hlasm"]
rootMarkers = [".hlasmplugin"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.hls]
cmd = ["haskell-language-server-wrapper", "--lsp"]
languages = ["haskell", "lhaskell"]
settings = { haskell = { cabalFormattingProvider = "cabal-fmt", formattingProvider = "ormolu" } }
[languageServers.home_assistant]
cmd = ["vscode-home-assistant", "--stdio"]
languages = ["yaml"]
rootMarkers = ["configuration.yaml", "configuration.yml"]
[languageServers.hoon_ls]
cmd = ["hoon-language-server"]
languages = ["hoon"]
rootMarkers = [".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.html]
languages = ["html"]
rootMarkers = ["package.json", ".git"]
initializationOptions = { configurationSection = ["html", "css", "javascript"], embeddedLanguages = { css = true, javascript = true }, provideFormatter = true }
[languageServers.htmx]
cmd = ["htmx-lsp"]
languages = ["aspnetcorerazor", "astro", "astro-markdown", "blade", "clojure", "django-html", "htmldjango", "edge", "eelixir", "elixir", "ejs", "erb", "eruby", "gohtml", "gohtmltmpl", "haml", "handlebars", "hbs", "html", "htmlangular", "html-eex", "heex", "jade", "leaf", "liquid", "markdown", "mdx", "mustache", "njk", "nunjucks", "php", "razor", "slim", "twig", "javascript", "javascriptreact", "reason", "rescript", "typescript", "typescriptreact", "vue", "svelte", "templ"]
rootMarkers = [".git"]
[languageServers.hydra_lsp]
cmd = ["hydra-lsp"]
languages = ["yaml"]
rootMarkers = [".git"]
[languageServers.hylo_ls]
cmd = ["hylo-language-server", "--stdio"]
languages = ["hylo"]
rootMarkers = [".git"]
[languageServers.hyprls]
cmd = ["hyprls", "--stdio"]
languages = ["hyprlang"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.idris2_lsp]
cmd = ["idris2-lsp"]
languages = ["idris2"]
[languageServers.intelephense]
cmd = ["intelephense", "--stdio"]
languages = ["php"]
rootMarkers = [".git", "composer.json"]
settings = { intelephense = { telemetry = { enabled = false } } }
[languageServers.janet_lsp]
cmd = ["janet-lsp", "--stdio"]
languages = ["janet"]
rootMarkers = ["project.janet", ".git"]
[languageServers.java_language_server]
cmd = ["java-language-server"]
languages = ["java"]
rootMarkers = ["build.gradle", "build.gradle.kts", "pom.xml", ".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.jdtls]
languages = ["java"]
rootMarkers = [["mvnw", "gradlew", "settings.gradle", "settings.gradle.kts", ".git"], ["build.xml", "pom.xml", "build.gradle", "build.gradle.kts"]]
[languageServers.jedi_language_server]
cmd = ["jedi-language-server"]
languages = ["python"]
rootMarkers = ["pyproject.toml", "setup.py", "setup.cfg", "requirements.txt", "Pipfile", ".git"]
[languageServers.jinja_lsp]
cmd = ["jinja-lsp"]
languages = ["jinja"]
rootMarkers = [".git"]
[languageServers.jls]
cmd = ["jls"]
languages = ["java"]
rootMarkers = ["pom.xml", "build.gradle", "build.gradle.kts", "settings.gradle", "settings.gradle.kts", "WORKSPACE", "WORKSPACE.bazel", ".java-version"]
[languageServers.jqls]
cmd = ["jq-lsp"]
languages = ["jq"]
rootMarkers = [".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.jsonls]
languages = ["json", "jsonc"]
rootMarkers = [".git"]
initializationOptions = { provideFormatter = true }
[languageServers.jsonnet_ls]
cmd = ["jsonnet-language-server"]
languages = ["jsonnet", "libsonnet"]
rootMarkers = ["jsonnetfile.json", ".git"]
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.julials]
cmd = ["julia", "--startup-file=no", "--history-file=no", "-e", "    # Load LanguageServer.jl: attempt to load from ~/.julia/environments/nvim-lspconfig\n    # with the regular load path as a fallback\n    ls_install_path = joinpath(\n        get(DEPOT_PATH, 1, joinpath(homedir(), \".julia\")),\n        \"environments\", \"nvim-lspconfig\"\n    )\n    pushfirst!(LOAD_PATH, ls_install_path)\n    using LanguageServer, SymbolServer, StaticLint\n    popfirst!(LOAD_PATH)\n    depot_path = get(ENV, \"JULIA_DEPOT_PATH\", \"\")\n    project_path = let\n        dirname(something(\n            ## 1. Finds an explicitly set project (JULIA_PROJECT)\n            Base.load_path_expand((\n                p = get(ENV, \"JULIA_PROJECT\", nothing);\n                p === nothing ? nothing : isempty(p) ? nothing : p\n            )),\n            ## 2. Look for a Project.toml file in the current working directory,\n            ##    or parent directories, with $HOME as an upper boundary\n            Base.current_project(),\n            ## 3. First entry in the load path\n            get(Base.load_path(), 1, nothing),\n            ## 4. Fallback to default global environment,\n            ##    this is more or less unreachable\n            Base.load_path_expand(\"@v#.#\"),\n        ))\n    end\n    @info \"Running language server\" VERSION pwd() project_path depot_path\n    server = LanguageServer.LanguageServerInstance(stdin, stdout, project_path, depot_path)\n    server.runlinter = true\n    run(server)\n  "]
languages = ["julia"]
rootMarkers = ["Project.toml", "JuliaProject.toml"]
[languageServers.just]
cmd = ["just-lsp"]
languages = ["just"]
rootMarkers = [".git"]
# WARN: no `filetypes` in source; set `languages` manually or this server never matches
[languageServers.kakehashi]
cmd = ["kakehashi"]
rootMarkers = ["kakehashi.toml", ".git"]
[languageServers.kcl]
cmd = ["kcl-language-server"]
languages = ["kcl"]
rootMarkers = [".git"]
[languageServers.koka]
cmd = ["koka", "--language-server", "--lsstdio"]
languages = ["koka"]
rootMarkers = [".git"]
[languageServers.kotlin_language_server]
cmd = ["kotlin-language-server"]
languages = ["kotlin"]
rootMarkers = ["settings.gradle", "settings.gradle.kts", "build.xml", "pom.xml", "build.gradle", "build.gradle.kts"]
[languageServers.kotlin_lsp]
cmd = ["intellij-server", "--stdio"]
languages = ["kotlin"]
rootMarkers = ["settings.gradle", "settings.gradle.kts", "pom.xml", "build.gradle", "build.gradle.kts", "workspace.json"]
[languageServers.kulala_ls]
cmd = ["kulala-ls", "--stdio"]
languages = ["http"]
rootMarkers = [".git"]
[languageServers.laravel_ls]
cmd = ["laravel-ls"]
languages = ["php", "blade"]
rootMarkers = ["artisan"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: offset_encoding was dropped; kakehashi manages offset encoding
[languageServers.lean3ls]
cmd = ["lean-language-server", "--stdio", "--", "-M", "4096", "-T", "100000"]
languages = ["lean3"]
[languageServers.lelwel_ls]
cmd = ["lelwel-ls"]
languages = ["llw"]
rootMarkers = [".git"]
[languageServers.lemminx]
cmd = ["lemminx"]
languages = ["xml", "xsd", "xsl", "xslt", "svg"]
rootMarkers = [".git"]
[languageServers.lexical]
cmd = ["lexical"]
languages = ["elixir", "eelixir", "heex", "surface"]
rootMarkers = ["mix.exs", ".git"]
# WARN: filetypes was empty in source; set `languages` manually or this server never matches
[languageServers.lsp_ai]
cmd = ["lsp-ai"]
initializationOptions = { memory = { file_store = {} }, models = {} }
# WARN: get_language_id (Lua function) was dropped
[languageServers.ltex]
cmd = ["ltex-ls"]
languages = ["bib", "gitcommit", "markdown", "org", "plaintex", "rst", "rnoweb", "tex", "pandoc", "quarto", "rmd", "context", "html", "xhtml", "mail", "text"]
rootMarkers = [".git"]
settings = { ltex = { enabled = ["bibtex", "gitcommit", "markdown", "org", "tex", "restructuredtext", "rsweave", "latex", "quarto", "rmd", "context", "html", "xhtml", "mail", "plaintext"] } }
# WARN: get_language_id (Lua function) was dropped
[languageServers.ltex_plus]
cmd = ["ltex-ls-plus"]
languages = ["asciidoc", "bib", "context", "gitcommit", "html", "markdown", "org", "pandoc", "plaintex", "quarto", "mail", "mdx", "rmd", "rnoweb", "rst", "tex", "text", "typst", "xhtml"]
rootMarkers = [".git"]
settings = { ltex = { enabled = ["asciidoc", "bib", "context", "gitcommit", "html", "markdown", "org", "pandoc", "plaintex", "quarto", "mail", "mdx", "rmd", "rnoweb", "rst", "tex", "latex", "text", "typst", "xhtml"] } }
[languageServers.lua_ls]
cmd = ["lua-language-server"]
languages = ["lua"]
rootMarkers = [[".emmyrc.json", ".luarc.json", ".luarc.jsonc"], [".luacheckrc", ".stylua.toml", "stylua.toml", "selene.toml", "selene.yml"], ".git"]
settings = { Lua = { codeLens = { enable = true }, hint = { enable = true, semicolon = "Disable" } } }
[languageServers.luau_lsp]
cmd = ["luau-lsp", "lsp"]
languages = ["luau"]
rootMarkers = [".git"]
[languageServers.lwc_ls]
cmd = ["lwc-language-server", "--stdio"]
languages = ["javascript", "html"]
rootMarkers = ["sfdx-project.json"]
initializationOptions = { embeddedLanguages = { javascript = true } }
[languageServers.m68k]
cmd = ["m68k-lsp-server", "--stdio"]
languages = ["asm68k"]
rootMarkers = ["Makefile", ".git"]
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.markdown_oxide]
cmd = ["markdown-oxide"]
languages = ["markdown"]
rootMarkers = [".git", ".obsidian", ".moxide.toml"]
[languageServers.marko-js]
cmd = ["marko-language-server", "--stdio"]
languages = ["marko"]
rootMarkers = [".git"]
[languageServers.marksman]
cmd = ["marksman", "server"]
languages = ["markdown", "markdown.mdx"]
rootMarkers = [".marksman.toml", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.matlab_ls]
cmd = ["matlab-language-server", "--stdio"]
languages = ["matlab"]
settings = { MATLAB = { indexWorkspace = true, installPath = "", matlabConnectionTiming = "onStart", telemetry = true } }
# WARN: before_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.mdx_analyzer]
cmd = ["mdx-language-server", "--stdio"]
languages = ["mdx"]
rootMarkers = ["package.json"]
initializationOptions = { typescript = {} }
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.mesonlsp]
cmd = ["mesonlsp", "--lsp"]
languages = ["meson"]
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
[languageServers.metals]
cmd = ["metals"]
languages = ["scala"]
rootMarkers = ["build.sbt", "build.sc", ["build.gradle", "build.gradle.kts"], "pom.xml"]
initializationOptions = { compilerOptions = { snippetAutoIndent = false }, isHttpEnabled = true, statusBarProvider = "show-message" }
[languageServers.microcad_lsp]
cmd = ["microcad-lsp", "--stdio"]
languages = ["microcad"]
rootMarkers = [".git"]
[languageServers.millet]
cmd = ["millet"]
languages = ["sml"]
rootMarkers = ["millet.toml"]
[languageServers.mint]
cmd = ["mint", "ls"]
languages = ["mint"]
rootMarkers = ["mint.json", ".git"]
[languageServers.mlir_lsp_server]
cmd = ["mlir-lsp-server"]
languages = ["mlir"]
rootMarkers = [".git"]
[languageServers.mlir_pdll_lsp_server]
cmd = ["mlir-pdll-lsp-server"]
languages = ["pdll"]
rootMarkers = ["pdll_compile_commands.yml", ".git"]
[languageServers.mm0_ls]
cmd = ["mm0-rs", "server"]
languages = ["metamath-zero"]
rootMarkers = [".git"]
[languageServers.mojo]
cmd = ["mojo-lsp-server"]
languages = ["mojo"]
rootMarkers = [".git"]
[languageServers.motoko_lsp]
cmd = ["motoko-lsp", "--stdio"]
languages = ["motoko"]
rootMarkers = ["dfx.json", ".git"]
initializationOptions = { formatter = "auto" }
[languageServers.move_analyzer]
cmd = ["move-analyzer"]
languages = ["move"]
rootMarkers = ["Move.toml"]
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.mpls]
cmd = ["mpls", "--theme", "dark", "--enable-emoji", "--enable-footnotes", "--no-auto"]
languages = ["markdown"]
rootMarkers = [".marksman.toml", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.msbuild_project_tools_server]
cmd = ["dotnet", "MSBuildProjectTools.LanguageServer.Host.dll"]
languages = ["msbuild"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.muon]
cmd = ["muon", "analyze", "lsp"]
languages = ["meson"]
[languageServers.mutt_ls]
cmd = ["mutt-language-server"]
languages = ["muttrc", "neomuttrc"]
rootMarkers = [".git"]
# WARN: no `cmd` in source; set `cmd` manually or kakehashi will skip this server
[languageServers.nelua_lsp]
languages = ["nelua"]
rootMarkers = ["Makefile", ".git", "*.nelua"]
[languageServers.neocmake]
cmd = ["neocmakelsp", "stdio"]
languages = ["cmake"]
rootMarkers = [".neocmake.toml", ".git", "build", "cmake"]
[languageServers.nextflow_ls]
cmd = ["nextflow-language-server"]
languages = ["nextflow"]
rootMarkers = ["nextflow.config", ".git"]
settings = { nextflow = { files = { exclude = [".git", ".nf-test", "work"] } } }
[languageServers.nextls]
cmd = ["nextls", "--stdio"]
languages = ["elixir", "eelixir", "heex", "surface"]
rootMarkers = ["mix.exs", ".git"]
[languageServers.nginx_language_server]
cmd = ["nginx-language-server"]
languages = ["nginx"]
rootMarkers = ["nginx.conf", ".git"]
[languageServers.nickel_ls]
cmd = ["nls"]
languages = ["ncl", "nickel"]
rootMarkers = [".git"]
[languageServers.nil_ls]
cmd = ["nil"]
languages = ["nix"]
rootMarkers = ["flake.nix", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.nim_langserver]
cmd = ["nimlangserver"]
languages = ["nim"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.nimls]
cmd = ["nimlsp"]
languages = ["nim"]
[languageServers.nixd]
cmd = ["nixd"]
languages = ["nix"]
rootMarkers = ["flake.nix", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.nomad_lsp]
cmd = ["nomad-lsp"]
languages = ["hcl.nomad", "nomad"]
[languageServers.ntt]
cmd = ["ntt", "langserver"]
languages = ["ttcn"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.nushell]
cmd = ["nu", "--lsp"]
languages = ["nu"]
[languageServers.nxls]
cmd = ["nxls", "--stdio"]
languages = ["json", "jsonc"]
rootMarkers = ["nx.json", ".git"]
# WARN: get_language_id (Lua function) was dropped
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.ocamllsp]
cmd = ["ocamllsp"]
languages = ["ocaml", "menhir", "ocamlinterface", "ocamllex", "reason", "dune"]
rootMarkers = [["dune-project", "dune-workspace"], ["*.opam", "opam", "esy.json", "package.json"], ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.ols]
cmd = ["ols"]
languages = ["odin"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
[languageServers.omnisharp]
cmd = ["omnisharp", "-z", "--hostPID", "28273", "DotNet:enablePackageRestore=false", "--encoding", "utf-8", "--languageserver"]
languages = ["cs", "vb"]
settings = { FormattingOptions = { EnableEditorConfigSupport = true }, MsBuild = {}, RenameOptions = {}, RoslynExtensionsOptions = {}, Sdk = { IncludePrereleases = true } }
[languageServers.opencl_ls]
cmd = ["opencl-language-server"]
languages = ["opencl"]
rootMarkers = [".git"]
[languageServers.openscad_ls]
cmd = ["openscad-language-server"]
languages = ["openscad"]
rootMarkers = [".git"]
[languageServers.openscad_lsp]
cmd = ["openscad-lsp", "--stdio"]
languages = ["openscad"]
rootMarkers = [".git"]
[languageServers.oso]
cmd = ["oso-cloud", "lsp"]
languages = ["polar"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.oxfmt]
languages = ["javascript", "javascriptreact", "typescript", "typescriptreact", "toml", "json", "jsonc", "json5", "yaml", "html", "vue", "handlebars", "css", "scss", "less", "graphql", "markdown"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: before_init (editor-side hook) was dropped; not applicable to kakehashi
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.oxlint]
languages = ["javascript", "javascriptreact", "typescript", "typescriptreact", "vue", "svelte", "astro"]
[languageServers.pact_ls]
cmd = ["pact-lsp"]
languages = ["pact"]
rootMarkers = [".git"]
[languageServers.panache]
cmd = ["panache", "lsp"]
languages = ["markdown", "quarto", "rmd"]
rootMarkers = [".panache.toml", "panache.toml", "_quarto.yml", "_bookdown.yml", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.pasls]
cmd = ["pasls"]
languages = ["pascal"]
[languageServers.pbls]
cmd = ["pbls"]
languages = ["proto"]
rootMarkers = [".pbls.toml", ".git"]
[languageServers.perlls]
cmd = ["perl", "-MPerl::LanguageServer", "-e", "Perl::LanguageServer::run", "--", "--port 13603", "--nostdio 0"]
languages = ["perl"]
rootMarkers = [".git"]
settings = { perl = { fileFilter = [".pm", ".pl"], ignoreDirs = ".git", perlCmd = "perl", perlInc = " " } }
[languageServers.perlnavigator]
cmd = ["perlnavigator"]
languages = ["perl"]
rootMarkers = [".git"]
[languageServers.perlpls]
cmd = ["pls"]
languages = ["perl"]
rootMarkers = [".git"]
settings = { perl = { perlcritic = { enabled = false }, syntax = { enabled = true } } }
[languageServers.pest_ls]
cmd = ["pest-language-server"]
languages = ["pest"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.phan]
cmd = ["phan", "-m", "json", "--no-color", "--no-progress-bar", "-x", "-u", "-S", "--language-server-on-stdin", "--allow-polyfill-parser"]
languages = ["php"]
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.phpactor]
cmd = ["phpactor", "language-server"]
languages = ["php"]
rootMarkers = [".git", "composer.json", ".phpactor.json", ".phpactor.yml"]
[languageServers.phpantom_lsp]
cmd = ["phpantom_lsp"]
languages = ["php"]
rootMarkers = [".phpantom.toml", ".git", "composer.json"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.phptools]
cmd = ["devsense-php-ls", "--stdio"]
languages = ["php"]
initializationOptions = { 0 = "{}" }
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.pico8_ls]
cmd = ["pico8-ls", "--stdio"]
languages = ["p8"]
[languageServers.please]
cmd = ["plz", "tool", "lps"]
languages = ["bzl"]
rootMarkers = [".plzconfig"]
[languageServers.pli]
cmd = ["pli_language_server"]
languages = ["pli"]
rootMarkers = [".pliplugin"]
# WARN: on_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.pony_language_server]
cmd = ["pony-lsp"]
languages = ["pony"]
rootMarkers = ["corral.json", ".git"]
settings = { pony-lsp = { defines = {}, ponypath = {} } }
[languageServers.pony_lsp]
cmd = ["pony-lsp"]
languages = ["pony"]
rootMarkers = ["corral.json", ".git"]
settings = { pony-lsp = { defines = {}, ponypath = {} } }
[languageServers.poryscript_pls]
cmd = ["poryscript-pls"]
languages = ["pory"]
rootMarkers = [".git"]
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.postgres_lsp]
cmd = ["postgres-language-server", "lsp-proxy"]
languages = ["sql"]
rootMarkers = ["postgres-language-server.jsonc"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.powershell_es]
languages = ["ps1"]
rootMarkers = ["PSScriptAnalyzerSettings.psd1", ".git"]
[languageServers.prismals]
cmd = ["prisma-language-server", "--stdio"]
languages = ["prisma"]
rootMarkers = [".git", "package.json"]
settings = { prisma = { prismaFmtBinPath = "" } }
[languageServers.prolog_ls]
cmd = ["swipl", "-g", "use_module(library(lsp_server)).", "-g", "lsp_server:main", "-t", "halt", "--", "stdio"]
languages = ["prolog"]
rootMarkers = ["pack.pl"]
[languageServers.prosemd_lsp]
cmd = ["prosemd-lsp", "--stdio"]
languages = ["markdown"]
rootMarkers = [".git"]
[languageServers.protols]
cmd = ["protols"]
languages = ["proto"]
rootMarkers = [".git"]
[languageServers.psalm]
cmd = ["psalm", "--language-server"]
languages = ["php"]
rootMarkers = ["psalm.xml", "psalm.xml.dist"]
[languageServers.pug]
cmd = ["pug-lsp"]
languages = ["pug"]
rootMarkers = ["package.json"]
[languageServers.puppet]
cmd = ["puppet-languageserver", "--stdio"]
languages = ["puppet"]
rootMarkers = ["manifests", ".puppet-lint.rc", "hiera.yaml", ".git"]
[languageServers.purescriptls]
cmd = ["purescript-language-server", "--stdio"]
languages = ["purescript"]
rootMarkers = ["bower.json", "flake.nix", "psc-package.json", "shell.nix", "spago.dhall", "spago.yaml"]
[languageServers.pylsp]
cmd = ["pylsp"]
languages = ["python"]
rootMarkers = ["pyproject.toml", "setup.py", "setup.cfg", "requirements.txt", "Pipfile", ".git"]
# WARN: cmd_env was dropped; kakehashi has no per-server environment option
[languageServers.pylyzer]
cmd = ["pylyzer", "--server"]
languages = ["python"]
rootMarkers = ["setup.py", "tox.ini", "requirements.txt", "Pipfile", "pyproject.toml", ".git"]
settings = { python = { checkOnType = false, diagnostics = true, inlayHints = true, smartCompletion = true } }
[languageServers.pyre]
cmd = ["pyre", "persistent"]
languages = ["python"]
rootMarkers = [".pyre_configuration"]
# WARN: on_exit (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.pyrefly]
cmd = ["pyrefly", "lsp"]
languages = ["python"]
rootMarkers = ["pyrefly.toml", "pyproject.toml", "setup.py", "setup.cfg", "requirements.txt", "Pipfile", ".git"]
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.pyright]
cmd = ["pyright-langserver", "--stdio"]
languages = ["python"]
rootMarkers = ["pyrightconfig.json", "pyproject.toml", "setup.py", "setup.cfg", "requirements.txt", "Pipfile", ".git"]
settings = { python = { analysis = { autoSearchPaths = true, diagnosticMode = "openFilesOnly", useLibraryCodeForTypes = true } } }
[languageServers.qmlls]
cmd = ["qmlls"]
languages = ["qml", "qmljs"]
rootMarkers = [".git"]
[languageServers.quick_lint_js]
cmd = ["quick-lint-js", "--lsp-server"]
languages = ["javascript", "typescript"]
rootMarkers = ["package.json", "jsconfig.json", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.r_language_server]
cmd = ["R", "--no-echo", "-e", "languageserver::run()"]
languages = ["r", "rmd", "quarto"]
[languageServers.racket_langserver]
cmd = ["racket", "--lib", "racket-langserver"]
languages = ["racket", "scheme"]
rootMarkers = [".git"]
# WARN: cmd was empty in source; set `cmd` manually or kakehashi will skip this server
[languageServers.raku_navigator]
languages = ["raku"]
rootMarkers = [".git"]
[languageServers.reason_ls]
cmd = ["reason-language-server"]
languages = ["reason"]
rootMarkers = ["bsconfig.json", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.regal]
cmd = ["regal", "language-server"]
languages = ["rego"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.regols]
cmd = ["regols"]
languages = ["rego"]
[languageServers.remark_ls]
cmd = ["remark-language-server", "--stdio"]
languages = ["markdown"]
rootMarkers = [".remarkrc", ".remarkrc.json", ".remarkrc.js", ".remarkrc.cjs", ".remarkrc.mjs", ".remarkrc.yml", ".remarkrc.yaml", ".remarkignore"]
[languageServers.rescriptls]
cmd = ["rescript-language-server", "--stdio"]
languages = ["rescript"]
rootMarkers = ["bsconfig.json", "rescript.json", ".git"]
initializationOptions = { extensionConfiguration = { allowBuiltInFormatter = true, askToStartBuild = false, cache = { projectConfig = { enabled = true } }, codeLens = true, incrementalTypechecking = { acrossFiles = true, enable = true }, inlayHints = { enable = true } } }
[languageServers.rls]
cmd = ["rls"]
languages = ["rust"]
rootMarkers = ["Cargo.toml"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.rnix]
cmd = ["rnix-lsp"]
languages = ["nix"]
# WARN: get_language_id (Lua function) was dropped
[languageServers.robotcode]
cmd = ["robotcode", "language-server"]
languages = ["robot", "resource"]
rootMarkers = ["robot.toml", "pyproject.toml", "Pipfile", ".git"]
[languageServers.robotframework_ls]
cmd = ["robotframework_ls"]
languages = ["robot"]
rootMarkers = ["robotidy.toml", "pyproject.toml", "conda.yaml", "robot.yaml", ".git"]
[languageServers.roc_ls]
cmd = ["roc_language_server"]
languages = ["roc"]
rootMarkers = [".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
[languageServers.rome]
languages = ["javascript", "javascriptreact", "json", "typescript", "typescriptreact"]
rootMarkers = ["package.json", "node_modules", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
# WARN: cmd_env was dropped; kakehashi has no per-server environment option
# WARN: commands (client-side user commands) were dropped
# WARN: handlers (client-side LSP handlers) were dropped
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
# WARN: on_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.roslyn_ls]
cmd = ["roslyn-language-server", "--stdio"]
languages = ["cs"]
settings = { "csharp|background_analysis" = { dotnet_analyzer_diagnostics_scope = "fullSolution", dotnet_compiler_diagnostics_scope = "fullSolution" }, "csharp|code_lens" = { dotnet_enable_references_code_lens = true }, "csharp|completion" = { dotnet_provide_regex_completions = true, dotnet_show_completion_items_from_unimported_namespaces = true, dotnet_show_name_completion_suggestions = true }, "csharp|inlay_hints" = { csharp_enable_inlay_hints_for_implicit_object_creation = true, csharp_enable_inlay_hints_for_implicit_variable_types = true, csharp_enable_inlay_hints_for_lambda_parameter_types = true, csharp_enable_inlay_hints_for_types = true, dotnet_enable_inlay_hints_for_indexer_parameters = true, dotnet_enable_inlay_hints_for_literal_parameters = true, dotnet_enable_inlay_hints_for_object_creation_parameters = true, dotnet_enable_inlay_hints_for_other_parameters = true, dotnet_enable_inlay_hints_for_parameters = true, dotnet_suppress_inlay_hints_for_parameters_that_differ_only_by_suffix = true, dotnet_suppress_inlay_hints_for_parameters_that_match_argument_name = true, dotnet_suppress_inlay_hints_for_parameters_that_match_method_intent = true }, "csharp|symbol_search" = { dotnet_search_reference_assemblies = true } }
[languageServers.rpmspec]
cmd = ["rpm_lsp_server", "--stdio"]
languages = ["spec"]
rootMarkers = [".git"]
[languageServers.rubocop]
cmd = ["rubocop", "--lsp"]
languages = ["ruby"]
rootMarkers = ["Gemfile", ".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: reuse_client was dropped; not applicable to kakehashi
[languageServers.ruby_lsp]
languages = ["ruby", "eruby"]
rootMarkers = ["Gemfile", ".git"]
initializationOptions = { formatter = "auto" }
[languageServers.ruff]
cmd = ["ruff", "server"]
languages = ["python"]
rootMarkers = ["pyproject.toml", "ruff.toml", ".ruff.toml", ".git"]
[languageServers.ruff_lsp]
cmd = ["ruff-lsp"]
languages = ["python"]
rootMarkers = ["pyproject.toml", "ruff.toml", ".git"]
[languageServers.rumdl]
cmd = ["rumdl", "server"]
languages = ["markdown"]
rootMarkers = [".git"]
[languageServers.rune_languageserver]
cmd = ["rune-languageserver"]
languages = ["rune"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: before_init (editor-side hook) was dropped; not applicable to kakehashi
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.rust_analyzer]
cmd = ["rust-analyzer"]
languages = ["rust"]
settings = { rust-analyzer = { lens = { debug = { enable = true }, enable = true, implementations = { enable = true }, references = { adt = { enable = true }, enumVariant = { enable = true }, method = { enable = true }, trait = { enable = true } }, run = { enable = true }, updateTest = { enable = true } } } }
[languageServers.salt_ls]
cmd = ["salt_lsp_server"]
languages = ["sls"]
rootMarkers = [".git"]
[languageServers.scheme_langserver]
cmd = ["scheme-langserver", "~/.scheme-langserver.log", "enable", "disable"]
languages = ["scheme"]
rootMarkers = ["Akku.manifest", ".git"]
[languageServers.scry]
cmd = ["scry"]
languages = ["crystal"]
rootMarkers = ["shard.yml", ".git"]
[languageServers.selene3p_ls]
cmd = ["selene-3p-language-server"]
languages = ["lua"]
rootMarkers = ["selene.toml"]
[languageServers.serve_d]
cmd = ["serve-d"]
languages = ["d"]
rootMarkers = ["dub.json", "dub.sdl", ".git"]
[languageServers.shopify_theme_ls]
cmd = ["shopify", "theme", "language-server"]
languages = ["liquid"]
rootMarkers = [".shopifyignore", ".theme-check.yml", ".theme-check.yaml", "shopify.theme.toml"]
[languageServers.shuck]
cmd = ["shuck", "server"]
languages = ["bash", "sh", "zsh"]
rootMarkers = [".shuck.toml", ".git"]
[languageServers.sixtyfps]
cmd = ["sixtyfps-lsp"]
languages = ["sixtyfps"]
[languageServers.slang_server]
cmd = ["slang-server"]
languages = ["systemverilog", "verilog"]
rootMarkers = [".git", ".slang"]
[languageServers.slangd]
cmd = ["slangd"]
languages = ["hlsl", "shaderslang"]
rootMarkers = ["slangdconfig.json", ".clang-format", ".git"]
[languageServers.slint_lsp]
cmd = ["slint-lsp"]
languages = ["slint"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: initializationOptions.storageDir (userdata) could not be represented and was omitted
[languageServers.smarty_ls]
cmd = ["smarty-language-server", "--stdio"]
languages = ["smarty"]
settings = { css = { validate = true }, smarty = { pluginDirs = {} } }
# WARN: unhandled field `message_level` (number) was dropped
[languageServers.smithy_ls]
cmd = ["cs", "launch", "--contrib", "smithy-language-server:0.8.0"]
languages = ["smithy"]
rootMarkers = ["smithy-build.json", "build.gradle", "build.gradle.kts", ".git"]
initializationOptions = { compilerOptions = { snippetAutoIndent = false }, isHttpEnabled = true, statusBarProvider = "show-message" }
[languageServers.snakeskin_ls]
cmd = ["snakeskin-cli", "lsp", "--stdio"]
languages = ["ss"]
rootMarkers = ["package.json"]
# WARN: initializationOptions.token (userdata) could not be represented and was omitted
[languageServers.snyk_ls]
cmd = ["snyk", "language-server", "-l", "info"]
languages = ["apex", "apexcode", "c", "cpp", "cs", "dart", "dockerfile", "elixir", "eelixir", "go", "gomod", "groovy", "helm", "java", "javascript", "json", "kotlin", "objc", "objcpp", "php", "python", "requirements", "ruby", "rust", "scala", "swift", "terraform", "terraform-vars", "typescript", "yaml"]
rootMarkers = [".git", ".snyk"]
initializationOptions = { activateSnykCode = "false", activateSnykIac = "true", activateSnykOpenSource = "true", integrationName = "Neovim", integrationVersion = "0.13.0-dev", trustedFolders = {} }
[languageServers.solang]
cmd = ["solang", "language-server", "--target", "evm"]
languages = ["solidity"]
rootMarkers = [".git"]
[languageServers.solargraph]
cmd = ["solargraph", "stdio"]
languages = ["ruby"]
rootMarkers = ["Gemfile", ".git"]
initializationOptions = { formatting = true }
settings = { solargraph = { diagnostics = true } }
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.solc]
cmd = ["solc", "--lsp"]
languages = ["solidity"]
[languageServers.solidity]
cmd = ["solidity-ls", "--stdio"]
languages = ["solidity"]
rootMarkers = [".git", "package.json"]
settings = { solidity = { includePath = "", remapping = {} } }
[languageServers.solidity_ls]
cmd = ["vscode-solidity-server", "--stdio"]
languages = ["solidity"]
rootMarkers = ["hardhat.config.js", "hardhat.config.ts", "foundry.toml", "remappings.txt", "truffle.js", "truffle-config.js", "ape-config.yaml", ".git", "package.json"]
[languageServers.solidity_ls_nomicfoundation]
cmd = ["nomicfoundation-solidity-language-server", "--stdio"]
languages = ["solidity"]
rootMarkers = ["hardhat.config.js", "hardhat.config.ts", "foundry.toml", "remappings.txt", "truffle.js", "truffle-config.js", "ape-config.yaml", ".git", "package.json"]
[languageServers.somesass_ls]
cmd = ["some-sass-language-server", "--stdio"]
languages = ["scss", "sass"]
rootMarkers = [".git", ".package.json"]
settings = { somesass = { suggestAllFromOpenDocument = true } }
[languageServers.sorbet]
cmd = ["srb", "tc", "--lsp"]
languages = ["ruby"]
rootMarkers = ["Gemfile", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
# WARN: get_language_id (Lua function) was dropped
[languageServers.sourcekit]
cmd = ["sourcekit-lsp"]
languages = ["swift", "objc", "objcpp", "c", "cpp"]
[languageServers.spade_ls]
cmd = ["spade-language-server"]
languages = ["spade"]
rootMarkers = ["swim.toml"]
[languageServers.spectral]
cmd = ["spectral-language-server", "--stdio"]
languages = ["yaml", "json", "yml"]
rootMarkers = [".spectral.yaml", ".spectral.yml", ".spectral.json", ".spectral.js"]
settings = { enable = true, run = "onType", validateLanguages = ["yaml", "json", "yml"] }
[languageServers.spyglassmc_language_server]
cmd = ["spyglassmc-language-server", "--stdio"]
languages = ["mcfunction"]
rootMarkers = ["pack.mcmeta"]
[languageServers.sqlls]
cmd = ["sql-language-server", "up", "--method", "stdio"]
languages = ["sql", "mysql"]
rootMarkers = [".sqllsrc.json"]
[languageServers.sqls]
cmd = ["sqls"]
languages = ["sql", "mysql"]
rootMarkers = ["config.yml"]
[languageServers.sqruff]
cmd = ["sqruff", "lsp"]
languages = ["sql"]
rootMarkers = [".sqruff", ".git"]
[languageServers.stan_ls]
cmd = ["stan-language-server", "--stdio"]
languages = ["stan"]
rootMarkers = [".git"]
[languageServers.standardrb]
cmd = ["standardrb", "--lsp"]
languages = ["ruby"]
rootMarkers = ["Gemfile", ".git"]
[languageServers.starlark_rust]
cmd = ["starlark", "--lsp"]
languages = ["star", "bzl", "BUILD.bazel"]
rootMarkers = [".git"]
[languageServers.starpls]
cmd = ["starpls"]
languages = ["bzl"]
rootMarkers = ["WORKSPACE", "WORKSPACE.bazel", "MODULE.bazel"]
[languageServers.statix]
cmd = ["statix", "check", "--stdin"]
languages = ["nix"]
rootMarkers = ["flake.nix", ".git"]
[languageServers.steep]
cmd = ["steep", "langserver"]
languages = ["ruby", "eruby"]
rootMarkers = ["Steepfile", ".git"]
[languageServers.stimulus_ls]
cmd = ["stimulus-language-server", "--stdio"]
languages = ["html", "ruby", "eruby", "blade", "php"]
rootMarkers = ["Gemfile", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.stylelint_lsp]
cmd = ["stylelint-language-server", "--stdio"]
languages = ["astro", "css", "html", "less", "scss", "vue"]
settings = { stylelint = { snippet = ["css", "postcss"], validate = ["css", "postcss"] } }
[languageServers.stylua]
cmd = ["stylua", "--lsp"]
languages = ["lua"]
rootMarkers = [".stylua.toml", "stylua.toml", ".editorconfig"]
[languageServers.stylua3p_ls]
cmd = ["stylua-3p-language-server"]
languages = ["lua"]
rootMarkers = [".stylua.toml", "stylua.toml"]
[languageServers.superhtml]
cmd = ["superhtml", "lsp"]
languages = ["superhtml", "html"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.svelte]
cmd = ["svelteserver", "--stdio"]
languages = ["svelte"]
settings = { typescript = { inlayHints = { enumMemberValues = { enabled = true }, functionLikeReturnTypes = { enabled = true }, parameterNames = { enabled = "literals", suppressWhenArgumentMatchesName = true }, parameterTypes = { enabled = true }, propertyDeclarationTypes = { enabled = true }, variableTypes = { enabled = true } } } }
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.svlangserver]
cmd = ["svlangserver"]
languages = ["verilog", "systemverilog"]
rootMarkers = [".svlangserver", ".git"]
settings = { systemverilog = { includeIndexing = ["*.{v,vh,sv,svh}", "**/*.{v,vh,sv,svh}"] } }
[languageServers.svls]
cmd = ["svls"]
languages = ["verilog", "systemverilog"]
rootMarkers = [".git"]
[languageServers.swift_mesonls]
cmd = ["Swift-MesonLSP", "--lsp"]
languages = ["meson"]
rootMarkers = ["meson.build", "meson_options.txt", "meson.options", ".git"]
[languageServers.syntax_tree]
cmd = ["stree", "lsp"]
languages = ["ruby"]
rootMarkers = [".streerc", "Gemfile", ".git"]
# WARN: on_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.systemd_ls]
cmd = ["systemd-lsp"]
languages = ["systemd"]
[languageServers.systemd_lsp]
cmd = ["systemd-lsp"]
languages = ["systemd"]
# WARN: filetypes was empty in source; set `languages` manually or this server never matches
[languageServers.tabby_ml]
cmd = ["tabby-agent", "--lsp", "--stdio"]
rootMarkers = [".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: before_init (editor-side hook) was dropped; not applicable to kakehashi
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.tailwindcss]
languages = ["aspnetcorerazor", "astro", "astro-markdown", "blade", "clojure", "django-html", "htmldjango", "edge", "eelixir", "elixir", "ejs", "erb", "eruby", "gohtml", "gohtmltmpl", "haml", "handlebars", "hbs", "html", "htmlangular", "html-eex", "heex", "jade", "leaf", "liquid", "markdown", "mdx", "mustache", "njk", "nunjucks", "php", "razor", "slim", "twig", "css", "less", "postcss", "sass", "scss", "stylus", "sugarss", "javascript", "javascriptreact", "reason", "rescript", "typescript", "typescriptreact", "vue", "svelte", "templ"]
settings = { tailwindCSS = { classAttributes = ["class", "className", "class:list", "classList", "ngClass"], includeLanguages = { eelixir = "html-eex", elixir = "phoenix-heex", eruby = "erb", heex = "phoenix-heex", htmlangular = "html", templ = "html" }, lint = { cssConflict = "warning", invalidApply = "error", invalidConfigPath = "error", invalidScreen = "error", invalidTailwindDirective = "error", invalidVariant = "error", recommendedVariantOrder = "warning" }, validate = true } }
[languageServers.taplo]
cmd = ["taplo", "lsp", "stdio"]
languages = ["toml"]
rootMarkers = [".taplo.toml", "taplo.toml", ".git"]
[languageServers.tblgen_lsp_server]
cmd = ["tblgen-lsp-server"]
languages = ["tablegen"]
rootMarkers = ["tablegen_compile_commands.yml", ".git"]
[languageServers.tclsp]
cmd = ["tclsp"]
languages = ["tcl", "sdc", "xdc", "upf"]
rootMarkers = ["tclint.toml", ".tclint", "pyproject.toml", ".git"]
[languageServers.teal_ls]
cmd = ["teal-language-server"]
languages = ["teal"]
rootMarkers = ["tlconfig.lua"]
[languageServers.templ]
cmd = ["templ", "lsp"]
languages = ["templ"]
rootMarkers = ["go.work", "go.mod", ".git"]
# WARN: no `filetypes` in source; set `languages` manually or this server never matches
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.termux_language_server]
cmd = ["termux-language-server"]
[languageServers.terraform_lsp]
cmd = ["terraform-lsp"]
languages = ["terraform", "hcl"]
rootMarkers = [".terraform", ".git"]
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.terraformls]
cmd = ["terraform-ls", "serve"]
languages = ["terraform", "terraform-vars"]
rootMarkers = [".terraform", ".git"]
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.texlab]
cmd = ["texlab"]
languages = ["tex", "plaintex", "bib"]
rootMarkers = [".git", ".latexmkrc", "latexmkrc", ".texlabroot", "texlabroot", "Tectonic.toml"]
settings = { texlab = { bibtexFormatter = "texlab", build = { args = ["-pdf", "-interaction=nonstopmode", "-synctex=1", "%f"], executable = "latexmk", forwardSearchAfter = false, onSave = false }, chktex = { onEdit = false, onOpenAndSave = false }, diagnosticsDelay = 300, formatterLineLength = 80, forwardSearch = { args = {} }, latexFormatter = "latexindent", latexindent = { modifyLineBreaks = false } } }
[languageServers.textlsp]
cmd = ["textlsp"]
languages = ["text", "tex", "org"]
rootMarkers = [".git"]
settings = { textLSP = { analysers = { languagetool = { check_text = { on_change = false, on_open = true, on_save = true }, enabled = true } }, documents = { org = { org_todo_keywords = ["TODO", "IN_PROGRESS", "DONE"] } } } }
[languageServers.tflint]
cmd = ["tflint", "--langserver"]
languages = ["terraform"]
rootMarkers = [".terraform", ".git", ".tflint.hcl"]
[languageServers.theme_check]
cmd = ["theme-check-language-server", "--stdio"]
languages = ["liquid"]
rootMarkers = [".theme-check.yml"]
[languageServers.thriftls]
cmd = ["thriftls"]
languages = ["thrift"]
rootMarkers = [".thrift"]
[languageServers.tilt_ls]
cmd = ["tilt", "lsp", "start"]
languages = ["tiltfile"]
rootMarkers = [".git"]
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.tinymist]
cmd = ["tinymist"]
languages = ["typst"]
rootMarkers = [".git"]
[languageServers.tofu_ls]
cmd = ["tofu-ls", "serve"]
languages = ["opentofu", "opentofu-vars", "terraform"]
rootMarkers = [".terraform", ".git"]
[languageServers.tombi]
cmd = ["tombi", "lsp"]
languages = ["toml"]
rootMarkers = ["tombi.toml", "pyproject.toml", ".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: commands (client-side user commands) were dropped
# WARN: handlers (client-side LSP handlers) were dropped
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.ts_ls]
languages = ["javascript", "javascriptreact", "typescript", "typescriptreact"]
initializationOptions = { hostInfo = "neovim" }
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.ts_query_ls]
cmd = ["ts_query_ls"]
languages = ["query"]
rootMarkers = [".tsqueryrc.json", ".git"]
initializationOptions = { parser_aliases = { ecma = "javascript", jsx = "javascript", php_only = "php" }, parser_install_directories = ["/Users/atusy/.local/share/nvim/site/parser"] }
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.tsgo]
languages = ["javascript", "javascriptreact", "typescript", "typescriptreact"]
settings = { typescript = { inlayHints = { enumMemberValues = { enabled = true }, functionLikeReturnTypes = { enabled = true }, parameterNames = { enabled = "literals", suppressWhenArgumentMatchesName = true }, parameterTypes = { enabled = true }, propertyDeclarationTypes = { enabled = true }, variableTypes = { enabled = true } } } }
[languageServers.tsp_server]
cmd = ["tsp-server", "--stdio"]
languages = ["typespec"]
rootMarkers = ["tspconfig.yaml", ".git"]
[languageServers.ttags]
cmd = ["ttags", "lsp"]
languages = ["ruby", "rust", "javascript", "haskell"]
rootMarkers = [".git"]
[languageServers.turbo_ls]
cmd = ["turbo-language-server", "--stdio"]
languages = ["html", "ruby", "eruby", "blade", "php"]
rootMarkers = ["Gemfile", ".git"]
[languageServers.turtle_ls]
cmd = ["node"]
languages = ["turtle", "ttl"]
rootMarkers = [".git"]
[languageServers.tvm_ffi_navigator]
cmd = ["python", "-m", "ffi_navigator.langserver"]
languages = ["python", "cpp"]
rootMarkers = ["pyproject.toml", ".git"]
[languageServers.twiggy_language_server]
cmd = ["twiggy-language-server", "--stdio"]
languages = ["twig"]
rootMarkers = ["composer.json", ".git"]
[languageServers.ty]
cmd = ["ty", "server"]
languages = ["python"]
rootMarkers = ["ty.toml", "pyproject.toml", "setup.py", "setup.cfg", "requirements.txt", ".git"]
[languageServers.typeprof]
cmd = ["typeprof", "--lsp", "--stdio"]
languages = ["ruby", "eruby"]
rootMarkers = ["Gemfile", ".git"]
# WARN: no `filetypes` in source; set `languages` manually or this server never matches
[languageServers.typos_lsp]
cmd = ["typos-lsp"]
rootMarkers = ["typos.toml", "_typos.toml", ".typos.toml", "pyproject.toml", "Cargo.toml"]
[languageServers.typst_lsp]
cmd = ["typst-lsp"]
languages = ["typst"]
rootMarkers = [".git"]
[languageServers.uiua]
cmd = ["uiua", "lsp"]
languages = ["uiua"]
rootMarkers = ["main.ua", ".fmt.ua", ".git"]
[languageServers.ungrammar_languageserver]
cmd = ["ungrammar-languageserver", "--stdio"]
languages = ["ungrammar"]
rootMarkers = [".git"]
settings = { ungrammar = { format = { enable = true }, validate = { enable = true } } }
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.unison]
cmd = ["nc", "localhost", "5757"]
languages = ["unison"]
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.unocss]
cmd = ["unocss-language-server", "--stdio"]
languages = ["erb", "haml", "hbs", "html", "css", "postcss", "javascript", "javascriptreact", "markdown", "ejs", "php", "svelte", "typescript", "typescriptreact", "vue-html", "vue", "sass", "scss", "less", "stylus", "astro", "rescript", "rust"]
rootMarkers = ["unocss.config.js", "unocss.config.ts", "uno.config.js", "uno.config.ts"]
[languageServers.uvls]
cmd = ["uvls"]
languages = ["uvl"]
rootMarkers = [".git"]
[languageServers.v_analyzer]
cmd = ["v-analyzer"]
languages = ["v", "vsh", "vv"]
rootMarkers = ["v.mod", ".git"]
[languageServers.vacuum]
cmd = ["vacuum", "language-server"]
languages = ["yaml.openapi", "json.openapi"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.vala_ls]
cmd = ["vala-language-server"]
languages = ["vala", "genie"]
[languageServers.vale_ls]
cmd = ["vale-ls"]
languages = ["asciidoc", "markdown", "text", "tex", "rst", "html", "xml"]
rootMarkers = [".vale.ini"]
# WARN: no `filetypes` in source; set `languages` manually or this server never matches
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.vectorcode_server]
cmd = ["vectorcode-server"]
[languageServers.verible]
cmd = ["verible-verilog-ls"]
languages = ["systemverilog", "verilog"]
rootMarkers = [".git"]
[languageServers.veridian]
cmd = ["veridian"]
languages = ["systemverilog", "verilog"]
rootMarkers = [".git"]
[languageServers.veryl_ls]
cmd = ["veryl-ls"]
languages = ["veryl"]
rootMarkers = [".git"]
[languageServers.vespa_ls]
cmd = ["java", "-jar", "vespa-language-server.jar"]
languages = ["sd", "profile", "yql"]
rootMarkers = [".git"]
[languageServers.vhdl_ls]
cmd = ["vhdl_ls"]
languages = ["vhd", "vhdl"]
rootMarkers = ["vhdl_ls.toml", ".vhdl_ls.toml"]
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.vimdoc_ls]
cmd = ["vimdoc-language-server"]
languages = ["help"]
rootMarkers = ["doc", ".git"]
[languageServers.vimls]
cmd = ["vim-language-server", "--stdio"]
languages = ["vim"]
rootMarkers = [".git"]
initializationOptions = { diagnostic = { enable = true }, indexes = { count = 3, gap = 100, projectRootPatterns = ["runtime", "nvim", ".git", "autoload", "plugin"], runtimepath = true }, isNeovim = true, iskeyword = "@,48-57,_,192-255,-#", runtimepath = "", suggest = { fromRuntimepath = true, fromVimruntime = true }, vimruntime = "" }
# WARN: no `cmd` in source; set `cmd` manually or kakehashi will skip this server
[languageServers.visualforce_ls]
languages = ["visualforce"]
rootMarkers = ["sfdx-project.json"]
initializationOptions = { embeddedLanguages = { css = true, javascript = true } }
[languageServers.vls]
cmd = ["v", "ls"]
languages = ["v", "vlang"]
rootMarkers = ["v.mod", ".git"]
# WARN: on_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.volar]
cmd = ["vue-language-server", "--stdio"]
languages = ["vue"]
rootMarkers = ["package.json"]
# WARN: on_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.vscoqtop]
cmd = ["vsrocqtop"]
languages = ["coq"]
rootMarkers = ["_RocqProject", "_CoqProject", ".git"]
[languageServers.vsrocq]
cmd = ["vsrocqtop"]
languages = ["coq"]
rootMarkers = ["_RocqProject", "_CoqProject", ".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
[languageServers.vtsls]
cmd = ["vtsls", "--stdio"]
languages = ["javascript", "javascriptreact", "typescript", "typescriptreact"]
initializationOptions = { hostInfo = "neovim" }
# WARN: on_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.vue_ls]
cmd = ["vue-language-server", "--stdio"]
languages = ["vue"]
rootMarkers = ["package.json"]
[languageServers.wasm_language_tools]
cmd = ["wat_server"]
languages = ["wat"]
[languageServers.wc_language_server]
cmd = ["wc-language-server", "--stdio"]
languages = ["html", "javascriptreact", "typescriptreact", "astro", "svelte", "vue", "markdown", "mdx", "javascript", "typescript", "css", "scss", "less"]
rootMarkers = ["wc.config.js", "wc.config.ts", "wc.config.mjs", "wc.config.cjs", "custom-elements.json", "package.json", ".git"]
initializationOptions = { hostInfo = "neovim" }
[languageServers.wgsl_analyzer]
cmd = ["wgsl-analyzer"]
languages = ["wgsl"]
rootMarkers = [".git"]
# WARN: cmd was a Lua function (dynamic command resolution); set `cmd` manually or kakehashi will skip this server
# WARN: on_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.yamlls]
languages = ["yaml", "yaml.docker-compose", "yaml.gitlab", "yaml.helm-values"]
rootMarkers = [".git"]
settings = { redhat = { telemetry = { enabled = false } }, yaml = { format = { enable = true } } }
[languageServers.yang_lsp]
cmd = ["yang-language-server"]
languages = ["yang"]
rootMarkers = [".git"]
[languageServers.yls]
cmd = ["yls", "-vv"]
languages = ["yar", "yara"]
rootMarkers = [".git"]
[languageServers.ziggy]
cmd = ["ziggy", "lsp"]
languages = ["ziggy"]
rootMarkers = [".git"]
[languageServers.ziggy_schema]
cmd = ["ziggy", "lsp", "--schema"]
languages = ["ziggy_schema"]
rootMarkers = [".git"]
# WARN: root_dir was a dynamic Lua function (dynamic root detection); approximate with `rootMarkers` if the default (client root) is wrong
# WARN: capabilities was dropped; kakehashi negotiates client capabilities itself
[languageServers.zizmor]
cmd = ["zizmor", "--lsp"]
languages = ["yaml"]
# WARN: on_attach (editor-side hook) was dropped; not applicable to kakehashi
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.zk]
cmd = ["zk", "lsp"]
languages = ["markdown"]
rootMarkers = [".zk"]
# WARN: workspace_required was dropped; not applicable to kakehashi
[languageServers.zls]
cmd = ["zls"]
languages = ["zig", "zir"]
rootMarkers = ["zls.json", "build.zig", ".git"]
[languageServers.zuban]
cmd = ["zuban", "server"]
languages = ["python"]
rootMarkers = ["pyproject.toml", "setup.py", "setup.cfg", "requirements.txt", "Pipfile", ".git"]
