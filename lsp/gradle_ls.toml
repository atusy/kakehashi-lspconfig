[languageServers.gradle_ls]
enabled = false
cmd = ["gradle-language-server"]
languages = ["groovy"]
rootMarkers = ["settings.gradle", "build.gradle"]
initializationOptions = { settings = { gradleWrapperEnabled = true } }
