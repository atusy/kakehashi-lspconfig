[languageServers.purescriptls]
enabled = false
cmd = ["purescript-language-server", "--stdio"]
languages = ["purescript"]
workspaceMarkers = ["bower.json", "flake.nix", "psc-package.json", "shell.nix", "spago.dhall", "spago.yaml"]
