# WARN: cmd hardcoded to `ruby-lsp`; source only added cwd, which kakehashi has no per-server option for
# WARN: reuse_client was dropped; not applicable to kakehashi
[languageServers.ruby_lsp]
enabled = false
cmd = ["ruby-lsp"]
languages = ["ruby", "eruby"]
workspaceMarkers = ["Gemfile", ".git"]
initializationOptions = { formatter = "auto" }
