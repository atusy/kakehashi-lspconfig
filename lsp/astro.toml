# WARN: cmd hardcoded to the global `astro-ls`; source preferred node_modules/.bin/astro-ls when present
# WARN: before_init (editor-side hook) was dropped; not applicable to kakehashi
[languageServers.astro]
enabled = false
cmd = ["astro-ls", "--stdio"]
languages = ["astro"]
workspaceMarkers = ["package.json", "tsconfig.json", "jsconfig.json", ".git"]
initializationOptions = { typescript = {} }
