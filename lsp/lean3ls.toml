# WARN: workspaceMarkers approximated; the elan stdlib special-case root (matching /lean/library in the path) is not representable and is dropped
# WARN: offset_encoding was dropped; kakehashi manages offset encoding
[languageServers.lean3ls]
enabled = false
cmd = ["lean-language-server", "--stdio", "--", "-M", "4096", "-T", "100000"]
languages = ["lean3"]
workspaceMarkers = ["leanpkg.toml", "leanpkg.path", ".git"]
