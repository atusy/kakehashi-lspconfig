# WARN: workspaceMarkers dropped 1 glob-style marker(s) (*.rego); kakehashi matches marker names exactly and does not glob-expand
[languageServers.regal]
enabled = false
cmd = ["regal", "language-server"]
languages = ["rego"]
workspaceMarkers = [".git"]
